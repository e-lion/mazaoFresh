extends layout
include mixins

block content

	form(name="adduser", method="post",action="/adduser")
		+field({ type: 'text', name: 'username', value: 'name', placeholder: 'username' })
		+field({ type: 'text', name: 'useremail', value: 'email@example.com', placeholder: 'usermail' })

		button#btnSubmit(type="submit", class="btn btn-info btn-lg") Submit

